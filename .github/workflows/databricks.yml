name: Databricks Deploy
on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: false

env:
  DATABRICKS_HOST: ${{ vars.DATABRICKS_HOST }}
  ARM_CLIENT_ID: ${{ secrets.ARM_CLIENT_ID }}
  ARM_CLIENT_SECRET: ${{ secrets.ARM_CLIENT_SECRET }}
  ARM_TENANT_ID: ${{ secrets.ARM_TENANT_ID }}

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: databricks/setup-cli@v0.299.1
      - name: Validate bundle
        run: databricks bundle validate -t dev
      - name: Notify Discord
        uses: sarisia/actions-status-discord@v1.16.0
        if: always()
        with:
          webhook: ${{ secrets.DISCORD_WEBHOOK }}
          title: "Validate"
  deploy:
    runs-on: ubuntu-latest
    needs: validate
    steps:
      - uses: actions/checkout@v6
      - uses: databricks/setup-cli@v0.299.1
      - name: Deploy bundle
        run: databricks bundle deploy -t dev
      - name: Get workspace URL
        id: ws
        if: success()
        run: |
          ROOT=$(databricks bundle validate -t dev -o json | jq -r '.workspace.root_path')
          echo "url=${DATABRICKS_HOST}/#workspace${ROOT}" >> "$GITHUB_OUTPUT"
      - name: Notify Discord
        uses: sarisia/actions-status-discord@v1.16.0
        if: always()
        with:
          webhook: ${{ secrets.DISCORD_WEBHOOK }}
          title: "Deploy (dev)"
          description: ${{ steps.ws.outputs.url && format('[Open in Databricks]({0})', steps.ws.outputs.url) || '' }}
