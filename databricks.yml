bundle:
  name: "shared_bundle"

include:
  - configuration/*.yml
  - configuration/*.yaml

targets:
  dev:
    default: true
    workspace:
      root_path: ${var.bundle_root_path}
    permissions:
      - group_name: "DATABRICKS_DMP_USER"
        level: CAN_MANAGE